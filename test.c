#include <stdio.h>
#include <math.h>

int main (int argc, char *argv[]) {
	//wall_y = cub->config.ry / (wall_d * cos(a - cub->player.angle));
	// float p_a =  -M_PI_2;
	// float a = p_a - 1.1F / 2;
	printf("%d\n",1000 / 0 * cos(1) );
   return 0;
}
